import React from 'react'

function Next_toggle({onClick}) {
  return (
        <i className="bi bi-caret-right-fill custom-next-button" onClick={onClick}>
          
        </i>    
  )
}

export default Next_toggle
